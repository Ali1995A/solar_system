# 太阳系页面升级执行报告 · 2026-09-07

## 执行依据与范围

依据：用户要求完善升级、纠正错误，追加核对公转/自转方向及提升天体造型/UI。执行者 Codex。模式 software_app，兼容 learning_system；见 project_mode.json。这是既有独立仓库，不是从父项目新建的派生工作区；父子记录不适用。

本报告为本任务初始执行报告，上游为本次用户请求，无另行计划文件；不替代其他报告。当前推荐阅读顺序：本报告 → README.md → AGENTS.md → index.html / surfaces.js / tests/check.cjs。下游为浏览器视觉验收，尚无下游报告。

## 完成内容

- 修正公转方向：+Y 为黄道北侧，z = -r sin(theta) 对应从北侧俯视逆时针。
- 自转按 NASA/JPL 恒星自转周期计算；金星与天王星通过大于 90° 的轴倾角表达逆行，不重复反转速度。新增方向与倾角信息。
- 月球轨道移到地球平移节点，与地球自转解耦；按约 27.3 天公转并保持同步朝向。土星环置于赤道面。
- 统一模拟日/秒，移除硬件性能对模拟速度的隐式修改；暂停和后台时间不推进。
- 新增本地生成纹理：地球示意陆地与云、木星条带和风暴、土星条带、岩质行星/月面坑纹、太阳表面细节。新增轴线与自转标记、太阳/月球选择、北侧俯视、全景、天体选择栏。
- 调整深色观测台风格、中文主控与移动端面板、焦点样式、显式折叠、跟随镜头，增加 CDN/WebGL 失败提示和模型局限说明。

## 文件与版本对账

所有路径相对 D:/Soft/MyCode/test-demo/solar-system/。

| 文件 | 动作 | 状态 |
| --- | --- | --- |
| index.html | 修改 | 当前推荐本地页面；语法及运动学检查通过，视觉待验收 |
| surfaces.js | 新增 | 当前页面配套纹理文件；语法检查通过 |
| tests/check.cjs | 新增 | 可重复运行的代码与运动学检查 |
| README.md | 修改 | 纠正单文件/离线误导及仓库身份，记录限制与数据源 |
| project_mode.json | 新增 | 项目模式记录 |
| AGENTS.md | 新增 | 持续维护交接入口 |
| docs/upgrade-20260907.md | 新增 | 当前执行报告 |

推荐版本已变为工作区 index.html + surfaces.js；旧版留在 Git HEAD 中，无另建近似文件副本。未删除文件，未提交、推送或部署。不存在需同步的 project_manifest.json / work_node_tree.json；未为补清单创建它们。未安装环境、依赖或素材，也无临时渲染目录。

## 交付与验证对账

代码修正、造型/UI实现、交接记录已完成。自动检查通过：JavaScript 语法、唯一 HTML ID、DOM引用、八大行星方向/周期、帧率独立性、暂停/后台时间、月球周期与结构、土星环平面。project_mode.json 解析与 Git 空白检查另行执行核验。Office 项目不适用。

浏览器视觉验收待完成：浏览器 URL 策略拒绝 file:///D:/Soft/MyCode/test-demo/solar-system/index.html，未绕过拒绝。当前检查不是浏览器运行、截图或 GPU 渲染检查。不能宣称所有交互和布局已在真浏览器中验证。

## 来源、结论与局限

NASA/JPL：https://ssd.jpl.nasa.gov/planets/phys_par.html （2026-09-07核对），提供恒星自转周期。NASA https://pwg.gsfc.nasa.gov/stargaze/SplanetsA.htm 支持轴倾角和金星/天王星逆行解释。方向公式及周期测试可信度高。

其他原有行星数据保留为近似值，并非完整星历审计。纹理为艺术示意；距离、体积、月球轨道比例经过夸张；太阳纹理静态，不模拟较差自转。高速下可能出现视觉混叠；星座不是真实天球坐标。

## 未解决事项与下一步

P1：获得允许的浏览器预览后检查桌面与手机布局、十个天体选择、缩放跟随、控件遮挡、加载失败及纹理表现。当前无法确认真实渲染质量。

P2：尚未验证 solar.linktime.link 当前线上源码与此仓库的绑定，也未发布。部署前确认该域名的部署入口，并确保 surfaces.js 同步发布。

继续工作无需重建环境或重复定位仓库；真实远程是 Ali1995A/solar_system，不是 Ali1995A/test-demo。先视觉验收，再根据授权处理发布。

## 推送补充记录 · 2026-09-07

上文为升级阶段记录；本节补充执行用户后续明确指令“推送”，更新其中尚未提交/推送的状态。当前推荐阅读顺序：本节 → 上文升级报告 → AGENTS.md → README.md。没有新增下游报告。

已核对 origin 为 https://github.com/Ali1995A/solar_system.git，当前分支 main；fetch 后提交前本地与远程无差异（0/0）。node tests/check.cjs 及 git diff --check 再次通过。7 个升级文件已提交为 add33ef（Improve solar system visuals and correct orbital simulation）。git push origin main 成功，远程返回 c791f45..add33ef main -> main，未使用强制推送。

本补充更新文件：AGENTS.md、docs/upgrade-20260907.md；属于交接记录更新，当前推荐交付物版本不变（add33ef 的 index.html + surfaces.js）。此前版本明确保留于 c791f45。无删除、环境安装或其他项目修改，父子关系与 Office 验收不适用。

浏览器视觉验收仍待完成，未核实网站自动部署是否触发或完成；GitHub 推送成功不等于线上验收通过。后续优先核对部署状态及浏览器显示，不需重复推送本次升级代码。

## 亮点与质感修正补充 · 2026-09-07

执行依据：用户指出地球亮点及造型缺少质感，明确要求修改后推送。执行者 Codex，项目模式与父子关系不变。本节补充上一轮升级与推送记录；推荐阅读顺序：本节 → 推送补充 → 初始升级报告 → AGENTS.md/README.md。

原因：每颗行星的自转教学标记使用白色 MeshBasicMaterial 小球，紧贴表面且默认可见；这是最直接对应所述亮点的代码来源，本轮未现场检查用户所见画面。已将标记及轴线默认隐藏，提供显式教学辅助开关。原始纹理硬边陆地、规则条带及统一材质削弱层次。

实施：地球海陆粗糙度、地表凹凸、独立云层及受光侧大气边缘；岩质天体柔化坑纹与凹凸；气态条带层次与木星风暴渐变；土星环径向纹理和缝隙；太阳自发光外观与颗粒表面、透明中心柔和日冕。纹理为程序生成艺术示意，非卫星影像；太阳仍为静态示意。

文件对账：修改 index.html、surfaces.js、tests/check.cjs、README.md、AGENTS.md、本报告；新增 tests/surfaces.cjs。当前推荐交付物变为这次质感修正版 index.html + surfaces.js，前版 add33ef/7ca983d 保留于 Git，原版 c791f45；没有近似副本、删除或依赖安装。架构仅需更新既有 AGENTS.md；Office、父子项目及下游报告不适用。

验证：node tests/check.cjs 通过（含辅助标记默认关闭回归）；既有 @napi-rs/canvas 执行 tests/surfaces.cjs 通过（十种纹理、贴图、云层透明范围、环缝与光晕中心）；git diff --check 通过。CPU 材质预览 D:/Soft/MyCode/.tmp/solar-materials-20260907.png 已人工查看，用于比较纹理，不是网页截图、不包含真实 WebGL 大气和土星环渲染。该文件位于工作区临时目录，不随仓库发布。完整浏览器验收仍未完成。

推送准备：fetch 后本地与 origin/main 差异 0/0。本轮已获明确推送授权；计划提交 "Refine planetary materials and hide teaching markers by default" 并普通推送 main，结果以下续推送记录为准。下一步核对推送结果与自动部署，再检查浏览器画面；无需再次定位仓库或安装环境。

## 日食月食追加实现 · 2026-09-07

上游：同轮质感修正任务尚未推送时，用户追加月球食影、日食/月食模拟及一键演示按钮；沿用“修改后推送”授权。材质修正已提交 434e83d。本节为同任务追加执行记录，补充上节；推荐阅读顺序为本节 → 亮点与质感修正 → 既有推送记录。

新增 eclipses.js、tests/eclipses.cjs；修改 index.html、tests/check.cjs、README.md、AGENTS.md 和本报告。当前推荐发布文件为 index.html + surfaces.js + eclipses.js，较材质版 434e83d 新增食影与演示控制；历史版本继续留在 Git。无删除、安装或临时服务。其他架构及 Office 检查不适用。

实现：逐表面位置计算太阳与遮挡天体的视半径和圆盘重叠率，用透明阴影层表达本影/半影；覆盖地球及云层、月球。月食暗红为大气折射的示意色，不声称物理光谱仿真。按钮自动对齐与切换近景，滑块手动演示、24秒过程播放，恢复按钮还原轨道、月球倾角/距离、镜头/跟随与暂停状态。切换天体/全景自动退出演示。月球直径相对地球按原有数据之比设置；正常轨道倾角约5.145°，演示临时置零与压缩距离。

验证：tests/check.cjs、tests/eclipses.cjs 通过；后者覆盖无食/偏食/全食/环形遮挡几何、演示尺度可形成全食、日月食对齐方向、切换、播放及恢复；shader 代码语法和页面引用检查不代替 GPU 编译。前节材质检查通过。完整浏览器画面仍待验收，特别是食影层、月食示意红色与移动端控件布局。

依据：https://science.nasa.gov/eclipses/geometry/ 与 https://science.nasa.gov/moon/eclipses/，2026-09-07 核对。模型是教学示意：不提供真实日期、路径、频率与时长预测；透明食影采用有限圆盘几何近似，不是预绘制黑斑或实时星历。计划把材质与食影提交一并普通推送 main，结果记录在后续推送核验。

### 本轮推送核验

git push origin main 成功，远程返回 7ca983d..5f6b208 main -> main；包含材质提交434e83d与食影演示提交5f6b208。未强推。当前推荐发布文件为5f6b208中的index.html、surfaces.js、eclipses.js，后续本记录提交只更新AGENTS.md和本报告，不改变交付物版本。浏览器视觉验收及网站自动部署状态依然待核实；不将GitHub推送成功解释为线上验收通过。

## 去云层与真实地球贴图 · 2026-09-07

依据：用户要求去除地球云层、修改后推送，并追加“地球的贴图要真实”。本节补充前述执行报告并取代早期地球云层/示意大陆说明；先读本节，再读日月食记录。项目模式不变，父子/Office不适用。

移除地球云网格、云纹理生成、虚构大陆和地形/粗糙度贴图。接入NASA Earth Observatory Blue Marble Next Generation 2004年7月无云全球合成图，保留原始JPEG，5400×2700、1,617,810字节。来源为 https://science.nasa.gov/earth/earth-observatory/blue-marble-next-generation/base-map/ ，精确下载链接和SHA256见assets/README.md。这是历史月度卫星合成影像，非实时照片；极地白色冰雪不是新增云层。

文件对账：修改index.html、surfaces.js、tests/surfaces.cjs、README.md、AGENTS.md及本报告；新增assets/earth-blue-marble-200407.jpg和assets/README.md。当前推荐版本为本次工作树三份程序文件加assets目录；旧版5f6b208/1d348e1保留在Git。未删除用户文件、安装运行时或依赖，仅下载本次请求所需NASA影像资产，约1.54MiB。

验证：NASA JPEG成功解码并目视检查，尺寸及SHA256核验；原运动学、日月食和九种程序材质检查通过；git diff --check通过。大气边缘与食影保留。浏览器加载/GPU显示仍待验收，在线部署未核实。fetch后本地与origin/main差异0/0，下一步按已获授权提交并推送；不重复安装环境，不恢复示意大陆或云层。

推送结果：d3cca2d（Use NASA cloud-free Earth imagery and remove cloud layer）已成功推送，远程返回1d348e1..d3cca2d main -> main。当前推荐交付版本为d3cca2d；本次后续记录仅更新交接与报告，不改变交付物版本。网站自动部署状态仍未核实。

## 月球与木星真实贴图修正 · 2026-09-07

用户询问月球、木星贴图真实性并要求修正后推送。检查确认两者原来均由 surfaces.js 程序生成，不是真实观测贴图。已接入 NASA SVS CGI Moon Kit 的 LROC WAC 自然色全球月球拼接图，以及 NASA Goddard/STScI Hubble 2015a 木星全球色彩图；来源、分辨率与精确 URL 记录在 assets/README.md。地球继续使用 NASA Blue Marble；其他天体仍为程序示意。

修改文件：index.html、README.md、assets/README.md、tests/surfaces.cjs、AGENTS.md、本报告；新增两张 NASA JPEG。当前推荐交付版本为三份程序文件加 assets 目录，d3cca2d 是此前地球版历史版本。NASA 图作为颜色贴图，关闭月球与木星的虚构 bump map；未声称是实时影像、真实当前经度或全波段科学产品。下载及纳入项目是用户明确要求真实贴图的一部分。

验证：三张资产成功解码（Earth 5400×2700，Moon 1024×512，Jupiter 1024×512）；check、eclipse、surface tests 与 diff check 均通过（浏览器 GPU 视觉验收仍待完成）。先前测试发现 surface 测试的资产检查缺少 root 定义，已修复并重新通过。准备提交并普通推送 main，结果以本节后续记录为准。
